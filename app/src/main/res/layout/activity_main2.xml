<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MainActivity" >

    <ImageButton
        android:id="@+id/gauche"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:background="@null"
        android:scaleType="fitCenter"
        android:src="@drawable/backward"
        android:layout_below="@+id/avant"
        android:layout_toLeftOf="@+id/avant"
        android:layout_toStartOf="@+id/avant" />

    <ImageButton
        android:id="@+id/droite"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:background="@null"
        android:scaleType="fitCenter"
        android:src="@drawable/forward"
        android:layout_centerVertical="true"
        android:layout_toRightOf="@+id/avant"
        android:layout_toEndOf="@+id/avant" />



    <ImageButton
        android:id="@+id/avant"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:background="@null"
        android:scaleType="fitCenter"
        android:src="@drawable/avant"
        android:layout_above="@+id/droite"
        android:layout_centerHorizontal="true" />

    <ImageButton
        android:id="@+id/arriere"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:background="@null"
        android:scaleType="fitCenter"
        android:src="@drawable/arriere"
        android:layout_below="@+id/gauche"
        android:layout_toRightOf="@+id/gauche"
        android:layout_toEndOf="@+id/gauche" />

</RelativeLayout>